# Auto detect text files and perform LF normalization
* text=auto

# =============================================================================
# Git LFS for vendored prebuilt binaries (Windows x64)
# These are large binary files that should be stored in LFS to keep repo fast
# =============================================================================

# vcpkg dependencies
prebuilt/x64-windows/**/*.lib filter=lfs diff=lfs merge=lfs -text
prebuilt/x64-windows/**/*.dll filter=lfs diff=lfs merge=lfs -text
prebuilt/x64-windows/**/*.exe filter=lfs diff=lfs merge=lfs -text
prebuilt/x64-windows/**/*.pdb filter=lfs diff=lfs merge=lfs -text

# OCC built components (cogutil, atomspace, moses, cogserver)
prebuilt/occ/**/*.lib filter=lfs diff=lfs merge=lfs -text
prebuilt/occ/**/*.dll filter=lfs diff=lfs merge=lfs -text
prebuilt/occ/**/*.exe filter=lfs diff=lfs merge=lfs -text
prebuilt/occ/**/*.pdb filter=lfs diff=lfs merge=lfs -text

# Static archives (if any)
prebuilt/**/*.a filter=lfs diff=lfs merge=lfs -text

# Note: If Git LFS is not installed, these will be stored directly in git
# which is fine - the repo will just be larger but builds will still be instant
